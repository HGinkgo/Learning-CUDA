cmake_minimum_required(VERSION 3.18)

set(LP_BACKEND "nvidia" CACHE STRING "GPU backend: nvidia, iluvatar, biren, or ascend")
set_property(CACHE LP_BACKEND PROPERTY STRINGS nvidia iluvatar biren ascend)

if(NOT LP_BACKEND STREQUAL "nvidia" AND NOT LP_BACKEND STREQUAL "iluvatar" AND
   NOT LP_BACKEND STREQUAL "biren" AND NOT LP_BACKEND STREQUAL "ascend")
    message(FATAL_ERROR "Unsupported LP_BACKEND='${LP_BACKEND}'")
endif()

if(LP_BACKEND STREQUAL "iluvatar")
    if(DEFINED ENV{COREX_VERSION} AND NOT "$ENV{COREX_VERSION}" STREQUAL "")
        set(_corex_version "$ENV{COREX_VERSION}")
    else()
        set(_corex_version "4.4.0")
    endif()
    set(ILUVATAR_SDK_ROOT "/usr/local/corex-${_corex_version}" CACHE PATH
        "Iluvatar CoreX SDK root")
    if(NOT EXISTS "${ILUVATAR_SDK_ROOT}/bin/clang++")
        message(FATAL_ERROR "Iluvatar CoreX compiler not found under ${ILUVATAR_SDK_ROOT}")
    endif()
    if(NOT DEFINED CMAKE_CXX_COMPILER)
        set(CMAKE_CXX_COMPILER "${ILUVATAR_SDK_ROOT}/bin/clang++" CACHE FILEPATH
            "Iluvatar host compiler")
    endif()
    if(NOT DEFINED CMAKE_CUDA_COMPILER)
        set(CMAKE_CUDA_COMPILER "${ILUVATAR_SDK_ROOT}/bin/clang++" CACHE FILEPATH
            "Iluvatar device compiler")
    endif()
endif()

if(LP_BACKEND STREQUAL "biren")
    if(DEFINED ENV{BIREN_HOME} AND NOT "$ENV{BIREN_HOME}" STREQUAL "")
        set(_biren_sdk_default "$ENV{BIREN_HOME}")
    else()
        set(_biren_sdk_default "/usr/local/birensupa/sdk/latest")
    endif()
    set(LP_BIREN_SDK_ROOT "${_biren_sdk_default}" CACHE PATH "BIRENSUPA SDK root")
    set(LP_BIREN_ARCH "br100" CACHE STRING "BIRENSUPA GPU architecture")
    if(NOT EXISTS "${LP_BIREN_SDK_ROOT}/supa/cmake/Modules/CMakeDetermineSUPACompiler.cmake")
        message(FATAL_ERROR "BIRENSUPA CMake modules not found under ${LP_BIREN_SDK_ROOT}")
    endif()
    list(PREPEND CMAKE_MODULE_PATH "${LP_BIREN_SDK_ROOT}/supa/cmake/Modules")
    set(CMAKE_SUPA_FLAGS "--supa-gpu-arch=${LP_BIREN_ARCH}")
    project(low_precision_reference LANGUAGES CXX SUPA)
elseif(LP_BACKEND STREQUAL "ascend")
    if(DEFINED ENV{ASCEND_HOME_PATH} AND NOT "$ENV{ASCEND_HOME_PATH}" STREQUAL "")
        set(_ascend_sdk_default "$ENV{ASCEND_HOME_PATH}")
    else()
        set(_ascend_sdk_default "/usr/local/Ascend/ascend-toolkit/latest")
    endif()
    set(LP_ASCEND_SDK_ROOT "${_ascend_sdk_default}" CACHE PATH "Ascend CANN toolkit root")
    set(LP_ASCEND_SOC "Ascend910B1" CACHE STRING "Ascend SoC version")
    if(NOT EXISTS "${LP_ASCEND_SDK_ROOT}/aarch64-linux/tikcpp/ascendc_kernel_cmake/ascendc.cmake")
        message(FATAL_ERROR "Ascend CANN Ascend C CMake support not found under ${LP_ASCEND_SDK_ROOT}")
    endif()
    project(low_precision_reference LANGUAGES CXX)
    if(NOT CMAKE_BUILD_TYPE)
        set(CMAKE_BUILD_TYPE Release CACHE STRING "Build type" FORCE)
    endif()
    set(CMAKE_POSITION_INDEPENDENT_CODE ON)
    set(ASCEND_CANN_PACKAGE_PATH "${LP_ASCEND_SDK_ROOT}")
    set(SOC_VERSION "${LP_ASCEND_SOC}")
    set(RUN_MODE npu)
    include("${LP_ASCEND_SDK_ROOT}/aarch64-linux/tikcpp/ascendc_kernel_cmake/ascendc.cmake")
else()
    project(low_precision_reference LANGUAGES CXX CUDA)
endif()

set(CMAKE_CXX_STANDARD 17)
set(CMAKE_CXX_STANDARD_REQUIRED ON)
set(CMAKE_CXX_EXTENSIONS OFF)
if(LP_BACKEND STREQUAL "nvidia" OR LP_BACKEND STREQUAL "iluvatar")
    set(CMAKE_CUDA_STANDARD 17)
    set(CMAKE_CUDA_STANDARD_REQUIRED ON)
    set(CMAKE_CUDA_EXTENSIONS OFF)
    if(NOT DEFINED CMAKE_CUDA_ARCHITECTURES)
        if(LP_BACKEND STREQUAL "iluvatar")
            set(CMAKE_CUDA_ARCHITECTURES ivcore11)
        else()
            set(CMAKE_CUDA_ARCHITECTURES 75;86)
        endif()
    endif()
endif()

set(LP_INCLUDE_DIR ${CMAKE_CURRENT_SOURCE_DIR}/include)
set(LP_NVIDIA_INCLUDE_DIR ${CMAKE_CURRENT_SOURCE_DIR}/backends/nvidia/include)
set(LP_BACKEND_INCLUDE_DIR ${LP_NVIDIA_INCLUDE_DIR})
set(LP_CUDA_SOURCE backends/nvidia/cuda_quant.cu)
set(LP_GPU_TEST_SOURCE tests/test_cuda.cu)
set(LP_BENCHMARK_SOURCE apps/benchmark.cu)
set(LP_CUDA_CLI_SOURCE apps/cuda_quant_cli.cu)
if(LP_BACKEND STREQUAL "iluvatar")
    set(LP_CUDA_SOURCE backends/iluvatar/cuda_quant.cu)
elseif(LP_BACKEND STREQUAL "biren")
    set(LP_BACKEND_INCLUDE_DIR ${CMAKE_CURRENT_SOURCE_DIR}/backends/biren/include)
    set(LP_CUDA_SOURCE backends/biren/supa_quant.su)
    set(LP_GPU_TEST_SOURCE backends/biren/test_cuda.su)
    set(LP_BENCHMARK_SOURCE backends/biren/benchmark.su)
    set(LP_CUDA_CLI_SOURCE backends/biren/cuda_quant_cli.su)
elseif(LP_BACKEND STREQUAL "ascend")
    set(LP_BACKEND_INCLUDE_DIR ${CMAKE_CURRENT_SOURCE_DIR}/backends/ascend/include)
    set(LP_CUDA_SOURCE backends/ascend/ascend_quant.cpp)
endif()

add_library(low_precision_reference common/quant.cpp)
target_include_directories(low_precision_reference PUBLIC ${LP_INCLUDE_DIR})

add_library(low_precision_io common/io.cpp)
target_include_directories(low_precision_io PUBLIC ${LP_INCLUDE_DIR})
target_link_libraries(low_precision_io PUBLIC low_precision_reference)

add_executable(test_cpu tests/test_cpu.cpp)
target_link_libraries(test_cpu PRIVATE low_precision_reference)

add_executable(test_precision_types tests/test_precision_types.cpp)
target_link_libraries(test_precision_types PRIVATE low_precision_reference)

add_executable(test_io tests/test_io.cpp)
target_link_libraries(test_io PRIVATE low_precision_io)
if(LP_BACKEND STREQUAL "ascend")
    target_compile_options(test_cpu PRIVATE -UNDEBUG)
    target_compile_options(test_precision_types PRIVATE -UNDEBUG)
    target_compile_options(test_io PRIVATE -UNDEBUG)
endif()

if(LP_BACKEND STREQUAL "biren")
    # BIRENSUPA's device linker must receive the .su object directly; it does
    # not extract device fatbins from a conventional static archive.
    add_library(low_precision_cuda OBJECT ${LP_CUDA_SOURCE})
else()
    add_library(low_precision_cuda ${LP_CUDA_SOURCE})
endif()
if(LP_BACKEND STREQUAL "ascend")
    target_sources(low_precision_cuda PRIVATE backends/ascend/ascend_runtime.cpp)
endif()
target_include_directories(low_precision_cuda PUBLIC
    ${LP_INCLUDE_DIR}
    ${LP_NVIDIA_INCLUDE_DIR}
    ${LP_BACKEND_INCLUDE_DIR})
if(LP_BACKEND STREQUAL "ascend")
    target_include_directories(low_precision_cuda PUBLIC
        "${LP_ASCEND_SDK_ROOT}/aarch64-linux/include")
endif()
target_link_libraries(low_precision_cuda PRIVATE low_precision_reference)
if(LP_BACKEND STREQUAL "iluvatar")
    target_link_directories(low_precision_cuda PUBLIC "${ILUVATAR_SDK_ROOT}/lib64")
    target_link_libraries(low_precision_cuda PUBLIC "${ILUVATAR_SDK_ROOT}/lib64/libcudart.so")
endif()
if(LP_BACKEND STREQUAL "ascend")
    ascendc_library(low_precision_ascend_kernels STATIC backends/ascend/kernels.cpp)
    target_include_directories(low_precision_cuda PRIVATE "${CMAKE_CURRENT_BINARY_DIR}/include")
    target_link_libraries(low_precision_cuda PRIVATE low_precision_ascend_kernels)
    target_link_libraries(low_precision_cuda PUBLIC ascendcl)
elseif(NOT LP_BACKEND STREQUAL "biren")
    set_target_properties(low_precision_cuda PROPERTIES CUDA_SEPARABLE_COMPILATION ON)
endif()

add_executable(test_cuda ${LP_GPU_TEST_SOURCE})
target_link_libraries(test_cuda PRIVATE low_precision_cuda low_precision_reference)
if(LP_BACKEND STREQUAL "nvidia" OR LP_BACKEND STREQUAL "iluvatar")
    set_target_properties(test_cuda PROPERTIES CUDA_SEPARABLE_COMPILATION ON)
elseif(LP_BACKEND STREQUAL "biren")
    set_target_properties(test_cuda PROPERTIES LINKER_LANGUAGE SUPA)
else()
    set_source_files_properties(${LP_GPU_TEST_SOURCE} PROPERTIES LANGUAGE CXX COMPILE_FLAGS "-x c++")
    target_compile_options(test_cuda PRIVATE -UNDEBUG)
endif()

add_executable(benchmark ${LP_BENCHMARK_SOURCE})
target_link_libraries(benchmark PRIVATE low_precision_cuda low_precision_reference)
if(LP_BACKEND STREQUAL "biren")
    target_compile_definitions(benchmark PRIVATE LP_GPU_BACKEND_NAME="biren")
elseif(LP_BACKEND STREQUAL "ascend")
    target_compile_definitions(benchmark PRIVATE LP_GPU_BACKEND_NAME="ascend")
endif()
if(LP_BACKEND STREQUAL "nvidia" OR LP_BACKEND STREQUAL "iluvatar")
    set_target_properties(benchmark PROPERTIES CUDA_SEPARABLE_COMPILATION ON)
elseif(LP_BACKEND STREQUAL "biren")
    set_target_properties(benchmark PROPERTIES LINKER_LANGUAGE SUPA)
else()
    set_source_files_properties(${LP_BENCHMARK_SOURCE} PROPERTIES LANGUAGE CXX COMPILE_FLAGS "-x c++")
endif()

add_executable(quant_cli apps/quant_cli.cpp)
target_link_libraries(quant_cli PRIVATE low_precision_io low_precision_reference)

add_executable(cuda_quant_cli ${LP_CUDA_CLI_SOURCE})
target_link_libraries(cuda_quant_cli PRIVATE low_precision_cuda low_precision_io low_precision_reference)
if(LP_BACKEND STREQUAL "nvidia" OR LP_BACKEND STREQUAL "iluvatar")
    set_target_properties(cuda_quant_cli PROPERTIES CUDA_SEPARABLE_COMPILATION ON)
elseif(LP_BACKEND STREQUAL "biren")
    set_target_properties(cuda_quant_cli PROPERTIES LINKER_LANGUAGE SUPA)
else()
    set_source_files_properties(${LP_CUDA_CLI_SOURCE} PROPERTIES LANGUAGE CXX COMPILE_FLAGS "-x c++")
endif()

enable_testing()
add_test(NAME cpu_reference COMMAND test_cpu)
add_test(NAME precision_types_cpu COMMAND test_precision_types)
add_test(NAME io_round_trip COMMAND test_io)
add_test(NAME cli_round_trip
         COMMAND ${CMAKE_CURRENT_SOURCE_DIR}/tests/test_cli.sh $<TARGET_FILE:quant_cli>)
add_test(NAME cuda_cli_round_trip
         COMMAND ${CMAKE_CURRENT_SOURCE_DIR}/tests/test_cuda_cli.sh
                 $<TARGET_FILE:cuda_quant_cli> $<TARGET_FILE:quant_cli>)
set_tests_properties(cuda_cli_round_trip PROPERTIES SKIP_RETURN_CODE 77)
add_test(NAME cuda_reference COMMAND test_cuda)
add_test(NAME benchmark_output
         COMMAND ${CMAKE_CURRENT_SOURCE_DIR}/tests/test_benchmark_output.sh $<TARGET_FILE:benchmark>)
add_test(NAME cli_benchmark_smoke
         COMMAND ${CMAKE_CURRENT_SOURCE_DIR}/apps/benchmark_cli.sh $<TARGET_FILE:quant_cli> 32 33)
